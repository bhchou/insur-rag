apiVersion: v1
kind: Secret
metadata:
  name: rag-secrets
type: Opaque
stringData:
  # 🔑 Google API Key 放這裡
  # 使用 stringData 就不用自己轉 base64，K8s 會自動幫您轉
  GOOGLE_API_KEY: "AIza..................."